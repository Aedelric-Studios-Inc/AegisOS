//! AegisOS image builder — assembles a flashable disk image.
fn main() {
    println!("[image-builder] Building AegisOS image...");
    // Read partitions.toml, create and populate disk image — TODO
}
