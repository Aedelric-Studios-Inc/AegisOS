//! AegisOS VPN service (WireGuard-compatible tunnel).
fn main() {
    println!("[vpn] service started");
    // Load VPN config, establish tunnel — TODO
    loop { std::thread::sleep(std::time::Duration::from_secs(10)); }
}
